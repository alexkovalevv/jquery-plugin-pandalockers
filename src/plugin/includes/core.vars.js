/*
 * Variables
 * Copyright 2014, OnePress, http://byonepress.com
 * 
 * @since 1.0.0
 * @pacakge includes

 * @!obfuscator:false
 * @!preprocess:false
 * @!priority:95
 * @!uglify:true
 * @!lang:[]
 * @!build:['free', 'premium', 'full-free', 'full-premium']
 */

(function($) {
	'use strict';

	if( !$.pandalocker ) {
		$.pandalocker = {};
	}

	if( !$.pandalocker.data ) {
		$.pandalocker.data = {};
	}
	if( !$.pandalocker.entity ) {
		$.pandalocker.entity = {};
	}
	if( !$.pandalocker.groups ) {
		$.pandalocker.groups = {};
	}
	if( !$.pandalocker.controls ) {
		$.pandalocker.controls = {};
	}
	if( !$.pandalocker.tools ) {
		$.pandalocker.tools = {};
	}
	if( !$.pandalocker.storages ) {
		$.pandalocker.storages = {};
	}
	if( !$.pandalocker.services ) {
		$.pandalocker.services = {};
	}
	if( !$.pandalocker.extras ) {
		$.pandalocker.extras = {};
	}

	if( !$.pandalocker.controls["social-buttons"] ) {
		$.pandalocker.controls["social-buttons"] = {};
	}
	if( !$.pandalocker.controls["connect-buttons"] ) {
		$.pandalocker.controls["connect-buttons"] = {};
	}
	if( !$.pandalocker.controls["subscription"] ) {
		$.pandalocker.controls["subscription"] = {};
	}

})(__$onp);
